
import clinicController from '../controllers/clinics'

function setupClinicRoutes(router){
	// router.get("/", clinicController.getMany)
	// router.post('/', clinicController.createOne)
	// router.get('/:id', clinicController.getOne)
	// router.put('/:id', clinicController.updateOne)
	// router.delete('/:id', clinicController.removeOne)
}

export default setupClinicRoutes
